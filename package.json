{
  "name": "your-pool-mate",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:marketing": "vite serve marketing --port 5174",
    "build": "vite build",
    "build:marketing": "rm -rf marketing-dist && mkdir -p marketing-dist && cp marketing/*.html marketing-dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.4.0"
  }
}
